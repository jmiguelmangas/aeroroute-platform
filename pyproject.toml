[project]
name = "aeroroute-platform-tools"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["pyyaml>=6.0"]

[dependency-groups]
dev = ["pytest>=8.0", "ruff>=0.11"]

[tool.ruff]
line-length = 80

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
