# AeroRoute MLX HLD

The source implementation guide is maintained with the project planning
material. This platform enforces its core boundaries: deterministic route
calculation stays in `aeroroute-optimizer`; MLX is explanation-only; weather
and persistence are API adapters; contracts are versioned separately.
